# 巡界 XUNJIE · 设备超级巡检人

**赛道：** 赛道二「传统行业 AI 解决方案」之 AI × 能源（新能源场站运维）  
**一句话：** 运维人员用自然语言下达任务后，巡界自动汇集异常、设备、空间、环境和 SOP 上下文，提出可追溯的现场检查建议；经用户授权，数字运维员在三维现场完成定位、巡检、取证与维修推演，并形成闭环回执。  
**在线演示：** https://rudy.cc.cd/xunjie/?key=GS5yYZzigSutoK （水/风/光三维场景；风电场景可语音指挥与问答）  
**代码开源：** https://github.com/NewRudy/xunjie

## 我们解决的具体问题

光伏异常发生后，运维人员往往需要在监控告警、资产台账、气象、SOP、三维地图和工单之间反复切换。真正的损耗不是“人不会走路”，而是信息与动作断链：设备难定位、到场依据不充分、检查过程不统一、证据与消缺结果没有回写。结果可能是无效到场、故障延误和无法复盘。

首版只打透一个可验证问题：`ANOM-DEMO-01` 组串电流异常如何转化为一次有依据、有授权、有证据、有回执的现场任务。

## 产品主链路

1. 用户说“检查 B2 屋顶异常”。
2. 巡界按事件装配目标组串、所属逆变器、资产拓扑、空间检查点、当日环境和相关 SOP，只保留本次任务需要的高信号上下文。
3. 系统给出“为什么需要到场、去哪里、要补哪些证据”的结构化建议，并标注数据来源及 `SIMULATED / MODELED / POLICY` 真值边界。
4. 用户说“我同意”。审批绑定唯一的 `approvalId + contextVersion + planHash`，过期或上下文变化后不能沿用。
5. 数字运维员沿登记路线跑到楼前和屋面检查点，聚焦目标部件；用户说“采集证据”，系统登记照片、热成像和读数仿真证据。
6. 用户再次确认后，任务与异常闭环，生成包含工单、证据、完成时间、闭环时累计损失（`SIMULATED`）、状态和来源的回执；设备卡与三维状态同步恢复。

同一语言入口还支持走、跑、飞、转向、跳跃、停止、回运维点和设备维修推演，用于把复杂空间任务变成可观察的数字现场作业。

## AI 真正做了什么

- **上下文工程：** 根据任务事件选择异常、设备、环境、空间和 SOP，而不是把全场遥测塞进提示词。
- **理解、研判与建议：** 大模型适配器既可把文字编排成受控人物命令，也可生成高层任务提案；两类输出都必须通过已登记语义 ID、可用能力、证据门槛和安全规则的确定性校验。没有模型凭据时，界面明确显示“确定性回退”，Demo 仍能稳定完成。
- **任务编排：** 模型只调用任务级能力，不逐帧控制人物；路线、动作、工单状态、证据门槛和数字计算由确定性程序负责。
- **人在回路：** 巡界可以主动建议，但数字现场动作与闭环都需要用户授权；未来真实高压设备写操作必须采用更高等级审批。

## 当前可运行原型

- **双场景数字现场**：风电（老鸦岭山地 3D Tiles + 10 台机组 + GPU 风场流线）沉浸式场景页 + 光伏园区业务闭环链路；设备、异常、检查点和路线使用统一语义 ID，场景以「场景包」数据登记（换场景 = 换数据）。
- 实际接入 `cesium-player-controller@0.2.0` 与 Rapier，人物走/跑/飞/转向/第三人称跟随；支持中文文字与「按住说话」语音（豆包 ASR，协议移植自管网智能体），回复按类型自动语音播报。
- **服务端受控编排（dispatch）**：闭环命令由服务端执行——演示复位+建任务原子化、审批服务端绑定、证据状态机裁决；会话轮次记忆 + 逐节点 trace 入库并随响应返回。
- **上下文问答**：身份/场景/任务/对象（状态、参数、尺寸）用人话回答（无 ID、数字可溯源，支持「它」指代）；门控未接住的问句由大模型按场景包事实组织语言。
- TypeScript MissionRuntime + SQLite 持久化，包含上下文装配、提案、绑定审批、事件回流、证据门槛和闭环回执。
- 质量防线 7 套件全本地化（不访问公网）：avatar 110、avatar-llm 98、model-gateway 32、dispatch 56、evals 40、voice 14、agent 72，全部通过。

## 数据、落地与价值验证

演示园区、设备运行与巡检证据均明确标为仿真；环境上下文来自公开天气接口并标为模型数据。原型不控制真实设备，也不宣称已经产生真实节省。

真实 PoC 的第一步不是重建整座场站，而是选一个逆变器或一类高频异常，导入匿名 SCADA/告警导出、资产映射、现行 SOP 和历史工单，与运维人员共同校准“是否值得到场、需要什么证据、何时算闭环”。上线后用以下指标验证价值：告警研判耗时、无效派工率、证据完整率、平均响应/修复时间和闭环率。

## 真实性边界

当前人物移动、证据采集和维修均为数字现场仿真，不代表机器人或真实设备已执行物理操作；场景坐标为虚构示意，不是工程测量成果。未来可在相同高层工具与审批合同下接入真实 SCADA、工单、无人机、机器人或 AR 作业终端。
