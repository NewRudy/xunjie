# 贵客松项目全局记忆

> 这是本项目的长期协作上下文。后续讨论、Kimi/智谱协作和实现前，先阅读本文件。只有重大决策变化才更新它，普通 UI 微调不单独记录。

## 1. 赛事与当前方向

- 赛事：贵客松·多彩贵州 AI 应用场景共创大赛（2026 数博会官方配套赛事，2026-08-27～30 贵阳，72 小时开发 + 100 天 PoC 孵化）。
- 赛道：赛道二「传统行业 AI 解决方案」之 **AI×能源** 赛题。命中子题：**企业节能 Agent + 园区能源优化 + 储能调度**。
- 项目名：**黔光智维**（英文代号 PECC，Park Energy Control Center，与 evcc 对偶：evcc 管用电，PECC 管园区发用电）。
- 一句话定位：**园区的发电管家——"我发电情况咋样、新政策下该咋办、哪台设备有问题、修好了没有、想扩大往哪装"。**
- 产品形态：借鉴 [evcc](https://github.com/evcc-io/evcc)（MIT）的交互与架构做**发电侧/园区版 evcc**，中文、自托管；UI 双层：大屏（3D 园区孪生）+ 集控中心（evcc 风格能量流/forecast/模式策略/告警）。
- 政策背景：发改价格〔2025〕136 号文后新能源上网电价全面市场化；国家能源局《分布式光伏发电开发建设管理办法》规定一般工商业"全部自发自用或余电上网"、大型工商业原则上全部自发自用（防逆流成刚需）。

## 2. 用户与痛点

- **确定性用户：工商业园区的业主/厂务与代运维师傅**（一人管多个屋顶/小站），次级：园区管委会、EMC 投资方。
- 真实园区原型（公开报道）：苏州工业园区光储充一体化、江门纸箱厂光储充、全国首批 52 个国家级零碳园区；贵州本土：**宁德时代贵州基地**（零碳工厂）、**贵阳贵安数据中心集群**（配套分布式光伏年发电超 5000 万 kWh，官方话术即"屋顶光伏+园区综合智慧能源+数字孪生能效可视化"）。
- 园区运维真实痛点：屋顶分散跑错楼（找设备=楼间寻路+找上屋顶通道）；屋面障碍物变化出热斑；高空作业安全；积灰清洗决策；业主看不懂告警；厂方/EMC/代运维三方要分账。

## 3. 产品主链路（五问闭环）

```text
① 发电咋样：楼→屋顶→逆变器→组串逐级分解，
   实发 vs 应发（真天气 Open-Meteo × PVGIS 模型 × 该屋顶朝向倾角装机）
② 价格怎么走、该咋办：贵州分时电价+代理购电月度价的价格曲线引擎
   × 光伏发电预测 × 园区负荷预测 → 逐时"自用/上网/充放"平衡策略卡
   （模式：自发自用余电上网 / 全额上网 / 防逆流 / 储能移峰 / 需量保护 / 现货竞价·远期）
③ 现状有啥问题：园区体检（效率/自用比例/防逆流触发/需量/力调/设备健康）问题清单带账
④ 去哪修：异常定位到楼和屋顶 → 生成现场任务 → 小人巡检（自动路线+检查点，
   含红外/安全确认）→ 证据留痕 → 消缺 → 发电恢复 → 闭环验证
⑤ 去哪装：新增设备选址建议（可装屋顶/储能增容点/充电桩布点），基于场景数据算账
→ 月度报告：按屋顶出账——发了多少、值多少、损失多少、巡了几次
```

- 主优先级：**运维（④）与发电/策略（①②③）是核心；小人是锦上添花层，主线通了再接。**
- AI 分工：确定性程序管算账、预测、策略、语义树（大模型不碰数字）；LLM 管报告叙事、政策问答（带文号出处）、EMC/合同条款分析；人在回路——系统只出建议，执行人确认。

## 4. 场景与设备

- 场景：以"贵阳贵安某产业园"为原型的**虚构示范园区**（明确标注虚构原型），CesiumJS + 天地图影像 + 公开 DEM + 程序化建筑群；屋顶光伏按轮廓/朝向/可用率程序化铺设。
- 设备目录（8 类，每类带语义卡：型号/参数/状态/今日数据）：光伏组件、组串式逆变器、并网箱、储能柜、充电桩（含光伏车棚）、箱变、总电表、环境监测仪。
- 语义命名树（全部业务的地基）：`园区 → 楼栋 → 屋顶子阵 → 逆变器 → 组串`，储能/充电桩/箱变/电表/气象仪同级挂园区；所有告警、任务、证据、报告挂树。
- 小人可达性：地面道路 + 楼前通道 + 屋面检查点；演示版"到楼前切屋面视角"，不做楼梯碰撞。
- 三维兜底：场景失败退二维天地图 + 屋顶轮廓，业务故事不能死在场景 bug 上。

## 5. 数据契约与真实性边界

**铁律：产品输入必须是公开数据或用户自有设备数据（自托管，数据不出场）。**

- 天气/辐照：Open-Meteo 免费 API，逐小时短波辐照，预报+历史存档（已实测贵阳：预报正常、历史 8-25 全天 4.30 kWh/m²）。
- 发电基准：PVGIS 免费 API（已实测贵阳 955 kWh/kWp/年）；Global Solar Atlas 交叉验证。
- 电价：贵州真实底数——黔发改价格〔2026〕421 号（两部制 10kV 电量电价 0.1238 元、需量 35 元/kW·月、容量 21.9 元/kVA·月，2026-08-01 起）、黔发改价格〔2023〕481 号（峰=平×1.6、谷=平×0.4；峰 10-13/17-22 时、谷 0-8 时）、代理购电逐月公告（0.389~0.401 元/kWh）；各省机制电价竞价结果公开。
- 真值标签：每个数字必须携带来源标签 `MEASURED（实测）/ MODELED（模型）/ SIMULATED（模拟）/ POLICY（政策）`。应发=MODELED（真天气真模型）；实发=SIMULATED（演示期仿真，UI 标注）；策略账=POLICY+确定性计算；巡检证据=SIMULATED。
- 真设备接入（PoC）：evcc 国产逆变器驱动（华为/阳光/锦浪等 Modbus 直连），sponsor 组件绕开，MIT 部分保留署名。
- 红线：不接私有单据；不碰受控测绘成果；不承诺签约级精度（屋顶净可用面积、屋面障碍物需现场复核）。

## 6. 72 小时切片与验收

| 阶段 | 内容 | 验收 |
|---|---|---|
| P0 冻结 | 共同合同：语义树、园区 fixture、数据契约、引擎 I/O、验收矩阵（本仓库 `contracts/`） | 三方确认 |
| P1 场景 | Cesium 园区：地形+影像+建筑群+屋顶光伏铺设+8 类设备+语义树绑定 | 浏览器流畅打开，点任何设备出信息卡 |
| P2 引擎 | Open-Meteo→应发模型；实发仿真器（标注）；贵州价格引擎；负荷仿真；策略卡计算 | 天气→应发曲线；策略账与手算一致 |
| P3 集控中心 | evcc 风格中文 UI：能量流+forecast+模式按钮+告警+策略卡 | 30 秒看懂 |
| P4 运维闭环 | 异常→任务→小人巡检→证据→消缺→恢复→月度报告 | 一键跑通全链路 |
| P5 打磨 | 演示脚本、2D 兜底、诚实标注检查 | 彩排两遍 |

- 技术栈：Vue3 + CesiumJS；确定性引擎 TypeScript（可后移 Go）；JSON fixtures + SQLite；LLM 走服务端代理、带确定性回退。
- 分工：场景组（P1+小人）、引擎数据组（P2）、UI 报告组（P3+P4 业务页）——Kimi/GPT/Claude 各领一组，对着 P0 合同验收；一个模型的产出未经验收不得成为另一个模型的事实基础。
- 100 天 PoC：真逆变器接入、贵阳真实园区试跑、政策盯梢自动化、储能策略上线、月度账单验证。

## 7. 重大更新规则

只有以下变化才更新本文件：目标用户或核心损耗改变；主链路改变；场景/数据架构改变；AI 范式或人工确认边界改变；协作分工改变；验收标准或数据真实性边界改变。每次更新应在文件末尾追加日期、变化、原因和影响，不覆盖历史判断。

---

## 重大更新记录

### 2026-08-28 · 初始版本

- 确定赛道二能源方向，优先聚焦集中式地面光伏场站运维。
- 将小人定位为数字现场作业层，主链路"告警 → 路线 → 到场 → SOP/清单 → 证据 → 人工确认 → 巡检记录"。
- 规定 Kimi 与智谱先分别讨论，冻结共同合同后再实现。

### 2026-08-28 · 执行优先级确认

- 第一优先级是数据基础：先做一小块真实、完整、可点选、可定位、可行走、可关联运维状态的光伏场站孪生场景，不追求全站大而全。
- 第二优先级是一到两个真实痛点：主痛点为"告警到现场巡检"，支撑痛点为"证据完整性与标准化记录"；AI 必须触发任务、检查项或记录状态变化，而不只是生成一段文字。
- Kimi 与 Claude Code 可以并行实现和比较，但必须使用同一份数据契约、状态机、模拟数据和验收矩阵；一个模型负责的代码不能未经验收成为另一个模型的事实基础。

### 2026-08-28 · 主命题重构（当前候选，待最终确认）

- 撤回把"小人巡检"作为产品主价值的表述。小人只是现场验证和空间关系的表现层；如果没有告警研判、派工决策、检查证据和结果回写，它只是三维游戏式 Demo。
- 当前更有说服力的主命题是「光伏场站 AI 告警研判与现场验证」，对外表达为：**先判断该不该跑，再告诉你怎么跑。**
- 核心业务损失改为：重复/误触发/受天气和整体工况影响的告警会造成无效研判和不必要到场；真正异常又可能因为缺少设备关系、历史上下文和标准检查证据而延误处理。
- 主链路改为：SCADA/气象/资产拓扑/历史事件 → AI 判断"观察、远程处理还是现场验证"并给出证据 → 需要到场时才启动三维定位、路线和小人 → 按 SOP 收集证据 → 人工确认并回写事件收据。
- 孪生场景的核心验收从"人物能不能走"提升为"资产、告警、拓扑、空间位置、路线和业务状态是否同源可追溯"；人物行走是其中一个可视化证明，不是独立功能。

### 2026-08-28 · 从赛事 Demo 转向真实行业产品（当前战略方向）

- 贵客松只是验证场，长期目标是用 AI 为新能源行业创造可度量的降本增效：减少发电损失、无效派工和人工研判时间，缩短故障处理闭环。
- 不再把"小人巡检"或"漂亮三维场景"作为核心价值。三维应服务于资产拓扑、损失空间定位和必要的现场验证；没有可量化的电量/工时/闭环改善，就不应投入大量建模。
- 当前最值得验证的产品方向是「光伏场站发电损失归因与运维优先级」：从 SCADA、辐照/气象、资产拓扑、告警和维护记录中识别异常，区分天气/数据质量/限电/设备不可用/疑似故障等原因，估算可避免损失，排序下一步运维动作；高优先级事件再进入三维定位和现场验证。
- 第一核心指标应是可追溯的损失与动作：估算损失电量、避免的无效派工、告警研判耗时、故障响应/修复时间、闭环率，而不是模型回答字数或场景复杂度。
- 第一阶段数据入口应接受一个场站或少量设备的匿名 SCADA/气象/告警/维护记录导出；模拟或回放数据只能证明算法链路，不能宣称真实收益。必须通过运维人员访谈和真实样本验证告警分类、损失估算和推荐动作。
- 不做另一个通用监控、告警、工单或巡检平台。已有行业系统已经覆盖相当一部分监测、异常、巡检配置和工单处理，差异化应放在跨数据源的损失归因、行动优先级和闭环验证。

### 2026-08-28 · 新候选：空间具身设备超级巡检人（待用户确认）

- 候选项目名：**巡界智维——水风光场站设备超级巡检人**；技术定位为“语音驱动的空间具身运维智能体”。产品框架可扩展到水电、风电和光伏，但贵客松首版只打透一个光伏设备故障，不同时制作三类场站。
- 核心价值不再表述为“小人能走、能飞、能变形”，而是：运维人员用自然语言下达任务后，系统自动汇集目标设备的实时参数、告警、环境、拓扑、BOM、历史记录和规程，在数字现场中完成定位、证据化诊断、部件级指导、人工确认和作业收据。
- 人形、车辆、无人机和设备透视/拆解模式必须对应真实作业能力：地面接近、高空/大范围巡视、长距离移动、不可见部件理解；首版建议只实现“人形 + 无人机/飞行 + 设备拆解视图”。
- 大语言模型只输出高层工具调用，例如 `go_to_asset`、`switch_mode`、`inspect_component`、`open_exploded_view`、`retrieve_sop` 和 `draft_receipt`；不得逐帧控制 W/A/S/D，也不得直接控制真实高压设备。路线、碰撞、动作、资产 ID、SOP 状态机和安全门槛由确定性程序执行。
- 候选首条主链路：语音要求检查某光伏逆变器降功率 → 汇集 SCADA/气象/相邻设备/历史告警 → AI 给出证据和置信度 → 角色选择合适形态并到达设备 → 拆解视图高亮疑似故障部件 → 检索并逐步执行安全 SOP → 人工确认模拟检修 → 生成可追溯记录。首选示例故障为逆变器散热风扇/风道异常导致过温降额。
- `cesium-player-controller` 可提供人物碰撞、第一/第三人称、飞行、车辆、模型切换、动画、外部输入和中心射线检测；但它不提供设备语义、自动路径规划、部件拆解、语音、Agent、设备上下文、SOP 或工单闭环，这些才是本项目需要形成的产品能力。
- 必须诚实区分“数字孪生中的检修模拟/现场指导”和“真实物理维修”。首版不宣称虚拟人物能够实际维修设备；未来只有接入机器人、无人机、AR 或受控设备接口后，才可能扩展为物理执行。

### 2026-08-28 · 方向拍板：屋顶光伏投资初筛孪生工作台

- **变化**：方向定为「贵阳屋顶光伏投资初筛孪生工作台」，取代前述场站方向候选。用户从场站值班员改为分布式光伏项目开发经理；核心损耗改为"136 号文后项目投资算不清账"；3D 场景为遮挡分析 + 选点算账工作台。
- **原因**：① 场站方向数据准入够不着；② 136 号文后屋顶算账所需数据全部公开；③ 3D 的存在理由是屋顶遮挡分析（对标 Aurora Solar）。
- **影响**：正文按新方向重写。"发电损失归因"与贵州"四窟窿"算账模型保留为扩展模块。

### 2026-08-28 · 定稿：黔光智维（园区版 evcc，当前有效）

- **变化**：在屋顶初筛方向上演进定稿为「黔光智维 PECC」——园区/光伏场站版 evcc（发电侧能源管家）。用户从开发经理改为园区业主/厂务与代运维；产品形态从"点屋顶出报告"升级为"园区五问闭环 + 大屏/集控中心双层 UI + 异常触发的小人巡检"；屋顶初筛降级为内置"去哪装"测算模块。
- **原因**：① 用户拍板借鉴 evcc（用电侧小而美）做发电侧；核心是"一个时间下不同情况、预测、价格调整、政策剧变痛点"；② 园区场景（建筑群+屋顶光伏+储能+充电桩）比地面场站和全城屋顶更可控、更有本土真实原型（贵阳贵安数据中心集群、宁德时代贵州基地）；③ 巡检小人在"发电异常→定位→出动"的触发器下获得真实存在理由，但定位为锦上添花层，主线是运维业务；④ 自托管 evcc 模式破解数据准入（软件去数据那里）。
- **影响**：正文第 1～6 节按定稿重写；P0 合同落盘本仓库 `contracts/`；执行以本文件正文为唯一合同，各模型开工前必读。

### 2026-08-28 · P0/P1 完成；产品名称复议中

- **P0 完成**：共同合同落盘 `contracts/`（README、semantic-tree、park-fixture、engine-io、data-contracts、acceptance-matrix）+ `data/fixtures/park-pecc-01.json` + 仓库 README/.gitignore。fixture 通过 JSON 解析校验。
- **P1 完成**：`web/`（Vite 8 + Vue 3.5 + TS + CesiumJS 1.144，手动静态拷贝 Cesium 而非 vite-plugin-cesium；禁用 ion）。场景 93 实体全部程序化生成自 fixture：6 栋楼、屋顶光伏阵列（深蓝 Canvas 纹理）、车棚光伏、储能集装箱、充电桩、箱变、道路、检查点；ENU 锚定 26.4085/106.5225/1280m；点击出语义卡（真值徽标 + "引擎未接入"占位）；五态状态着色集中 `web/src/constants/colors.ts`（INV-B-02 默认 fault 红对应 demoAnomaly）；"虚构园区 · 仿真数据"水印常驻；§P1 验收 6 行全过（无头 Chrome CDP 截图实证）。底图 OSM 在线/离线降级可切换，来源在关于面板列明。遗留：在线 OSM 瓦片未端到端目检；STR-B2-07 无独立 3D 标记（P4 细化）；语义 ID 挂 `properties.semanticId`，`entityRegistry` 导出在 `window.__pecc`。
- **名称复议**：用户指出"黔光智维"不合适——①不止光伏（是光储充荷的园区能源系统）②不止贵州（政策包可按省扩展）。候选：园能智维（推荐）/综能智维/能枢/源策；英文 PECC（Park Energy Control Center）不变，代码与 API 不受影响，待用户拍板后全局替换中文名。
- **P2 开工**：确定性引擎（engine-io.md + data-contracts.md 为唯一规格）并行开发中，与命名解耦。

### 2026-08-28 · P4 候选升级：`巡界`设备超级巡检人（待用户拍板）

- 当前有效产品方向仍是 PECC 园区光储充荷能源管家，P1/P2 及共同合同不推翻；“超级巡检人”优先作为 P4 运维闭环的空间具身智能体层，而不是另起一个只有自由漫游的新产品。
- 建议将智能体命名为 **巡界**，完整表达为“巡界·设备超级巡检人”；整项目可对外表达为“园区能源经营大脑 + 巡界空间执行智能体”。
- 第一条演示链直接复用现有 `STR-B2-07` 异常、`INV-B-02` 状态、B02 楼前/屋面检查点和巡检状态机：语音下达检查任务 → Agent 汇集损失金额、SCADA/气象/相邻组串、资产与 SOP → 调用高层空间工具 → 人形/飞行形态到达 → 热成像/读数证据 → 部件级拆解或电气关系高亮 → 人工确认 → 现有引擎 close 并验证曲线恢复。
- 形态变化必须映射作业能力：人形用于地面接近和交互，无人机/飞行用于屋顶或高处，车辆用于大范围场站，拆解/透视属于设备理解视图。首版只做“人形 + 飞行 + 一个部件拆解视图”。
- LLM 只调用高层工具和解释证据，不逐帧操纵人物、不生成经营数字、不绕过现有任务状态机；确定性引擎继续负责金额、状态迁移、证据门槛与闭环恢复。
- 若后续用户明确决定把“巡界”升级为跨水风光独立产品，需要先重开范围冻结，重新定义主用户、首个场景、数据契约和验收矩阵；在此之前不得让多模型并行修改当前 PECC 核心合同。

### 2026-08-28 · 巡界项目定义文档落盘

- 将最新讨论整理到 `XUNJIE_PROJECT_BRIEF.md`，作为“巡界是谁、上下文是什么、AI 如何建议、用户如何授权、数字人如何执行”的协作草案。
- 当前一句话口径为：**巡界是一名由大模型负责理解、推理与任务编排的数字运维人员：它基于用户任务或场站异常，融合设备运行、空间场景与环境信息，主动提出可执行建议，经用户授权后在数字现场执行巡检、诊断和运维作业流程，并形成可追溯记录。**
- 文档明确：巡界是 PECC 的空间执行智能体；大模型负责理解、研判、建议和高层工具编排，确定性程序负责数字、路线、碰撞、动作、任务状态和安全门槛；首版执行边界是数字现场作业，不宣称物理维修。

### 2026-08-28 · 多能源场景架构扩展候选（待确认）

- 用户提出水电、风电、光伏三类场景都应可进入，现有 PECC 可能不足以作为顶层产品。候选架构调整为：**巡界**作为跨水风光的数字运维人员和 Agent 交互层，PECC 作为园区光储充荷/光伏业务适配器，黔风智维作为风电场景适配器。
- CesiumJS/3D Tiles 负责统一三维地理运行时和内容规范，不提供现成的完整水风光场站资产库。应在 Cesium 之上建立 `ScenePackage`，统一管理地形/3D Tiles、设备模型、可拆解模型、碰撞网格、语义 ID、路线、环境、数据绑定和许可信息。
- 风电场景复用黔风智维是可行且优先的路径：现有仓库已有山地 3D Tiles、空间机组、风场可视化、BIM 构件选择/拆解和状态绑定；但现有场景仍有空间演示资产、源数据资产和 BIM 资产的边界，且模型动作/构件映射需要专业复核，不能直接宣称工程级孪生。
- 三类场景都可做入口和浏览，但只做一条完整的部件级巡检/运维链；建议风电和光伏二选一做深，水电先做可浏览原型和上下文适配，不在比赛首版实现三套完整 SOP。
- 在用户确认多场景方向前，不修改 PECC 的数据合同、P2 引擎接口和验收矩阵；各模型先按 `ScenePackage` 和统一 `assetId/componentId` 讨论，不要分别重写顶层架构。

### 2026-08-28 · 巡界 Agent 编排与上下文工程基线

- 已完成一次面向 Agent workflow、context engineering、HITL、持久化、MCP 和工业数字孪生的一手资料调研，并核查后续 Claude Code 维护的管网智能体实现。管网项目当前采用单一受控 LangGraph、能力注册表、结构化业务请求、结果包、结构化前端动作、SQLite checkpoint 和回归评测；展示 worktree 相关 Agent 测试本次实跑 `62 passed`。这些模式可迁移，管网本身的业务实现不能直接当作巡界的实时具身执行器。
- 巡界的正式编排原则确定为：**事件驱动上下文装配 → 大模型结构化研判/建议/高层计划 → 能力/数据时效/安全确定性校验 → 人工审批中断 → 高层场景工具执行 → 场景事件和证据回流 → 闭环或升级**。不做把全场景和全量遥测塞入 prompt，也不让模型逐帧控制人物。
- 状态分为三层：Cesium 高频 `Scene Runtime State`、可持久化的 `Mission State`、每次调用临时生成的 `LLM Context`。模型只能看到按事件挑选的最小高信号摘要；引擎可持有完整场景状态但不等于必须传给模型。
- 自动上下文事件至少包括 `mission_created`、`scene_entered`、`asset_focused`、`anomaly_changed`、`approaching_checkpoint`、`component_focused`、`environment_changed`、`evidence_captured` 和用户追问。相机位置只是候选线索，必须解析成已登记的 `assetId/componentId/checkpointId` 才能成为业务上下文。
- 首版工具只允许面向任务的高层能力：`get_asset_context`、`get_environment_operability`、`get_related_assets`、`get_relevant_steps`、`focus_asset`、`navigate_to_checkpoint`、`switch_form`、`capture/evaluate_evidence`、`advance`、`close_or_escalate`。不暴露 W/A/S/D、逐帧运动、任意坐标/节点、SQL 或真实高风险设备写操作。
- “同意”必须绑定唯一未过期的 `approvalId + contextVersion + planHash`；环境/告警变化使旧批准失效。读操作可自动执行，数字现场移动和模拟检修按计划粒度审批，未来真实设备动作必须更高等级审批。
- 当前 TypeScript 项目先做类型化 `MissionRuntime`/状态机，不立即引入多智能体框架；保留未来采用 JS/TS LangGraph 等持久化运行时的可能性，但先冻结 `contracts/agent-context.md`、`agent-state.md`、`agent-tools.md`、`scene-events.md` 和回放 fixtures。Kimi 负责场景/事件桥，Claude Code 负责 Agent runtime/适配器/评测，不能各自修改共享合同。
- 详细设计落在 `XUNJIE_AGENT_ORCHESTRATION.md`，后续重大编排变化先更新该文档和本节，再通知各模型。

### 2026-08-28 · Kimi/Claude 第一条巡界纵向切片完成

- 已按共同合同并行调用 Kimi Code 与 Claude Code：Kimi 完成 `web/src/agent/` 场景事件桥、任务面板、命令消费和数字运维员仿真；Claude 完成 `engine/src/agent/` MissionRuntime、上下文装配、planner/LLM 适配器、SQLite 持久化和 HTTP 路由。Codex 负责跨模型契约校验、联调修复和验收。
- 当前首条可演示路径已经跑通：`ANOM-DEMO-01` → `STR-B2-07` → 用户审批 → `CP-B02-FRONT` → `CP-B02-ROOF` → photo/thermal/reading 仿真证据 → 二次闭环确认 → `TASK-DEMO-01 resolved`；闭环回执包含异常撤销和损失固化，数字均带 `SIMULATED` 边界。
- 实现中的关键安全边界已落地：模型只产生/消费高层语义命令；路线和到达由前端确定性执行；后端以既有 `inspection.ts` 状态机为唯一业务迁移源；审批绑定 `approvalId + contextVersion + planHash + TTL`；证据缺少屋面检查点时不能闭环；重复事件按幂等键无副作用。
- 已修复的联调问题：业务目标 `STR-B2-07` 原先没有前端语义实体；楼前到达后的下一跳命令没有继续消费；证据事件前后端原先一方发送数组、一方读取单条；前端审批 warning 会随每次快照重复刷屏。当前组串三维标记由其登记逆变器位置派生，明确标为“位置示意”，不能当真实组串坐标。
- 验收证据：`engine` TypeScript 检查通过；`web` TypeScript 检查和生产构建通过；`engine/scripts/agent-test.mjs` **68/68** 通过；浏览器端完成全链路回放。默认无模型凭据时 UI 显示“模型不可用 · 确定性回退”，不伪称在线。
- 当前边界与下一步：场景仍是虚构园区 fixture，证据是仿真引用，真实 SCADA/历史工单/现场照片和真实 LLM 尚未接入；下一步应先用真实运维样本复核“告警是否值得到场、需要哪些证据、修复后如何确认”，再决定是否扩展 `cesium-player-controller` 的飞行/形态和水风光场景包。不得因为纵向切片跑通就宣称生产落地。

### 2026-08-28 · 联调验收收口

- 前端补齐高层场景命令的消费回执：`focus_asset` 对应 `asset_focused`，`show_component` 对应 `component_focused`；后端确认后从待执行队列移除，避免后续审批快照重复执行旧命令。
- 演示移动采用明确的数字现场加速档，前端日志与 UI 保持“仿真动作”标注；最终浏览器验收在清空演示数据后完成，日志无重复历史 warning，闭环后 `pendingCommands=[]`、巡检任务 `resolved`、异常 `anomalyResolved=true`。

### 2026-08-29 · 数字运维员自然语言移动与维修 Demo 完成

- 用户将近期 Demo 的最高优先级明确为“能控制一个可见的小人在工程场景中完成各种移动并维修”；语音采集暂后置，先跑通同一入口的文字自然语言版本。战略产品价值仍是运维业务闭环，人物动作是当前展演主角，不等同于真实物理作业。
- 新增共同合同 `contracts/avatar-command.md`：`POST /api/agent/avatar/interpret` 只返回受控高层动作，覆盖登记点走/跑/飞、相对前后左右与升降、转向、跳跃、停止、回运维点、设备聚焦和维修仿真；距离/角度有界，未知目标必须澄清，不允许脚本、任意坐标或真实设备写操作。后续 ASR 只把转写文本送入该接口，不重写动作链。
- 分工按用户指定落地：Kimi Code 负责 `web/` 的本地 GLB 数字人、第三人称跟随、动作执行、控制面板和维修特效；Claude Code 负责 `engine/` 的确定性中文解析、统一结果外壳和最小测试；Codex 负责动作合同、联调、浏览器验收和收口。
- 当前可见能力：人物以 WALK/RUN/FLY/JUMP/REPAIR 状态移动；飞行采用抬升—巡航—下降轨迹；“飞到设备并维修”会到 `CP-INV-B02`、聚焦 `STR-B2-07`，展示报警脉冲、部件拆解/展开、维修进度和外观恢复。人物模型已本地化，来源与 CC-BY-4.0 署名记录在 `web/public/vendor/NOTICE.md`，运行不依赖外网模型。
- 最小验收已完成：后端 `avatar-test` 42/42 通过，前端 TypeScript 检查通过；浏览器连续跑通“跑到 B2 楼前 → 飞到 B2 屋顶 → 飞到设备并维修”，维修日志最终出现恢复完成，控制台无 error。演示页保留“数字现场仿真，不控制真实设备”常驻标识。
- 真实边界：当前没有麦克风/ASR，没有 3D Tiles 碰撞体、重力和工程级寻路，人物动作是 Cesium 插值与动画，维修是数字现场可视化而非物理维修；已保留 `AvatarMotionController` 适配接口，后续再接 `cesium-player-controller`/Rapier 或真实机器人、无人机与 AR。

### 2026-08-29 · 贵客松持续打磨窗口（当前执行指令）

- 用户要求项目持续推进至 2026-08-30 08:00，Sol Max 只负责简化规划、合同和最终验收，Kimi Code 负责前端，Claude Code 负责后端；减少测试，不做复杂架构，目标是一个“场景过得去、主链路有说服力”的贵客松 Demo。
- 执行计划落在 `NIGHT_RUN_PLAN.md`。本轮只打透光伏 `ANOM-DEMO-01 → STR-B2-07 → INV-B-02`，不扩水风光三套场景、不做完整物理碰撞和真实设备控制。
- 北极星链路冻结为：语言下达检查任务 → 汇集设备/异常/环境/SOP 上下文 → AI 提出需要到场的受控建议 → 用户结构化同意 → 数字运维员跑/飞到设备 → 证据与维修仿真 → 回执闭环。语音只是语言输入适配层，审批不得绕过现有四元绑定。
- 最小验证规则：每个阶段只做相关类型检查/受控指令单测，最终只做一次浏览器主链路；非致命问题记录，不在截止前重构。

### 2026-08-29 · 巡界语言入口接入安全任务闭环

- 语言入口不再只控制人物移动和维修特效，新增三个受控任务意图：启动 `ANOM-DEMO-01` 巡检、同意/拒绝当前唯一待审批项、在屋面检查点采集 photo/thermal/reading 仿真证据；自然语言仍先经后端确定性解释，前端只调用既有 MissionRuntime，不绕过审批绑定。
- Kimi Code 在 `web/` 增加可选 Web Speech API 中文语音适配；最终转写复用文字入口，不支持或未授权麦克风时明确降级为文字。Claude Code 在 `engine/` 增加对应中文映射、澄清规则与最小测试。
- 当前文字主链已在浏览器实跑通过：`检查 B2 屋顶异常 → 我同意 → CP-B02-FRONT → CP-B02-ROOF → 采集证据 → 我同意 → resolved`。后端 avatar 指令测试 66/66、前后端 TypeScript 检查通过；没有主动触发麦克风权限。
- 下一演示缺口已锁定为两个：闭环回执需要醒目展示；重复演示前需要显式复位 demo 数据，避免已消缺异常产生历史状态提示。

### 2026-08-29 · 研判依据与闭环回执可视化完成

- 前端将原始上下文整理为异常、设备、环境、SOP/证据四类研判卡，展示后端原始字段、可用性、`SIMULATED/MODELED/POLICY` 与来源数量；原始 11～12 项上下文仍可折叠查看。未配置模型凭据时继续显示“确定性回退”，不伪称在线大模型结论。
- 后端 Mission 快照补齐 `observationRefs/evidenceRefs/sourceRefs`，继续在顶层返回 `inspectionTask` 与 `receipt`；闭环卡展示工单/异常状态、证据数量与类型、完成时间及带 `SIMULATED` 标签的累计损失，不新增接口或虚构收益。
- 语言启动演示前会调用既有 `/api/debug/reset`，清空上次任务并重新注入 demo 异常，同时把数字人重置到运维点；重复演示不再出现上次已消缺状态的历史 warning。
- 验收证据：`agent-test` 72/72，前后端 TypeScript 检查通过；浏览器第二次完整跑通语言主链，最终显示 `TASK-DEMO-01 resolved`、`ANOM-DEMO-01 resolved`、3 件 photo/thermal/reading 仿真证据和闭环回执。

### 2026-08-29 · 贵客松首屏与维修 wow moment 收口

- 演示页顶层品牌调整为“巡界 XUNJIE · 设备超级巡检人 · 光伏数字现场”，隐藏未完成的大屏、集控中心、巡检和报告占位入口，只保留单一数字现场主链。
- Agent 面板将八步计划默认折叠，首屏优先展示任务、建议/审批或闭环回执、研判依据；闭环后的上下文明示为“决策时快照”，避免其历史故障状态与当前恢复状态混淆。
- 前端设备状态改为与后端回执一致：演示复位后 `STR-B2-07/INV-B-02` 恢复 fault，收到 `anomalyStatus=resolved` 回执后才变为 normal，三维颜色和设备卡同步；InfoCard 不再显示过时的“引擎未接入 P2”。维修动画本身不篡改任务状态。
- 浏览器验收已连续通过：建议/审批首屏可见 → 两段路线到屋面 → 三类证据与闭环 → 设备卡恢复正常；随后语言指令“飞到 B2 逆变器维修 7 号异常组串”完整返回 navigate/focus/repair 三命令，人物进入 REPAIR、报警定位后正常恢复 IDLE。

### 2026-08-29 · 提交材料与 90 秒讲解冻结

- 新增 `SUBMISSION_ONE_PAGER.md`，以具体运维断点、六步主链、AI/确定性分工、当前实现、PoC 第一步、价值指标和真实性边界组织一页作品说明；README 已改为巡界主入口，包含双终端冷启动、四句任务链、维修指令、最小验证和可选 OpenAI-compatible 模型接法。
- 新增 `DEMO_SCRIPT_90S.md`，按实测约 20 秒巡检跑位与约 13 秒飞行维修推演压缩口播，保留五句输入、60 秒按钮兜底和三类评委追问答案；不再把证据闭环表述为已经物理维修。
- 无上下文 Claude 读者测试首轮发现赛道名、回执口径、物理维修误解、默认回退态和 90 秒超时五个问题；修订后二次测试为 `PASS`，估算口播约 293 字，动作窗口覆盖已知跑位/维修耗时。
- 当前最大非代码风险仍是现场默认没有模型凭据：页面会诚实显示确定性回退。后端真实 OpenAI-compatible 适配器已存在，若赛场配置 Kimi/智谱临时凭据，模型提案仍需通过语义 ID、数字溯源、证据和动作白名单校验；密钥不得写入仓库或截图。

### 2026-08-29 · 预冻结冷启动检查

- 使用临时 SQLite 与独立端口 `8791` 从 `pnpm start` 启动全新后端实例，成功完成 PVGIS/Open-Meteo 标定加载，`GET /api/health`、`POST /api/debug/reset` 与自然语言 `start_inspection ANOM-DEMO-01` 响应均正常；检查后已停止进程并删除临时数据库，未触碰演示库。
- `web` 的 `pnpm build` 成功，Cesium 静态资源进入 `dist/cesium`；主 JS 约 4.25 MB（gzip 约 1.16 MB）是当前已知非致命体积问题，截止前不做拆包重构。
- 前后端 `tsc --noEmit` 均通过；后端受控语言指令 `avatar-test` **66/66** 通过，覆盖启动巡检、结构化同意/拒绝、采集三类证据、人物跑/飞/相对移动和维修仿真，同时验证未知目标与多意图必须澄清。
- 当前 git 工作树在检查前后均干净。最终 E 检查仅保留到截止前：一次浏览器主链、演示端口和提交状态确认。

### 2026-08-29 · 用户纠偏：文字大模型与人物控制器必须真实接入

- 用户明确语音后续再改，当前主入口是文字对话框；核心链路必须是“文字 → 大模型结构化高层命令 → 确定性白名单校验 → `cesium-player-controller` 执行人物动作”。
- 复核确认此前实现存在两处实质缺口：`web` 只使用自写 `AvatarActor` 并注释“后续可替换”，没有安装 `cesium-player-controller`；`POST /api/agent/avatar/interpret` 明确是无 LLM 的确定性中文解析。原先“只剩最终主链”的预冻结结论因此撤销。
- 已核查上游仓库当前 npm 版本 `0.2.0`、MIT 许可及核心 API：`init/update/setInput/reset/getPosition/getIsFlying/changeView/playAnimation` 等；本轮实际接入控制器，但不扩展完整 3D Tiles 碰撞源。
- 共享合同已升级为 `contracts/avatar-command.md` v1.1：有 Kimi/智谱 OpenAI-compatible 凭据时真实调用模型并校验，无凭据或校验失败时保留诚实的确定性回退；前端须分别展示本轮人物指令解释来源和执行器来源。

### 2026-08-29 · 纠偏实现与浏览器整链验收完成

- 前端已实际安装并初始化 `cesium-player-controller@0.2.0` 与其 Rapier peer dependency，使用上游示例动画人物承载 IDLE/WALK/RUN/FLY/JUMP；通过公开 `addStaticColliders` 接口加载本地最小地面网格。运行时面板明确显示执行器来源，同时声明未接建筑、屋面和 3D Tiles 工程碰撞。
- 后端 `POST /api/agent/avatar/interpret` 已改为 LLM-first：配置 `AGENT_LLM_API_KEY/BASE_URL/MODEL` 时真实调用 OpenAI-compatible `chat/completions`，模型 JSON 必须整批通过 kind、字段、登记 ID、距离/角度、证据和数量白名单；无凭据、HTTP/超时/JSON/校验失败时整批丢弃并如实标记确定性回退。
- 用户最新范围已落实：文字对话框是主入口；语音折叠为“后续能力”，不属于本轮完成定义。人物解释来源与任务提案模型状态分开显示，默认验收机无模型凭据时页面显示 `NO_CREDENTIALS`，不冒充在线 LLM。
- 浏览器实跑通过人物链：`跑到 B2 楼前` 显示 RUN 并沿道路移动；`飞到 B2 屋顶` 显示 FLY；`飞到 B2 逆变器维修 7 号异常组串` 生成 navigate/focus/repair 三命令并完成报警、拆解/进度与恢复仿真。
- 同一文字框完整跑通任务链：`检查 B2 屋顶异常 → 我同意 → CP-B02-FRONT → CP-B02-ROOF → 采集证据 → 我同意`，最终 `TASK-DEMO-01` 与 `ANOM-DEMO-01` 均 resolved，回执含 photo/thermal/reading 三件仿真证据。前端生产构建及前后端 TypeScript 通过；测试为 avatar 66/66、avatar-llm 92/92、agent 72/72。
- 真实限制：当前没有真实模型凭据、真实 SCADA/工单/现场证据或工程碰撞；人物路径使用 fixture 登记折线，维修是数字现场可视化。下一轮不应再把这些边界包装成生产级具身控制。
