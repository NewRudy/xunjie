# 巡界 · 贵客松持续打磨计划

> 执行窗口：2026-08-29 00:53 至 2026-08-30 08:00（Asia/Shanghai）  
> 模式：Sol Max 负责范围、合同与最终验收；Kimi Code 只负责 `web/`；Claude Code 只负责 `engine/`。自动推进，少量检查，每个可演示阶段一次提交。

## 北极星

做成一条 90 秒内讲明白、现场稳定可跑的主链路：

> 光伏设备出现异常后，运维员不用在监控、资产台账、SOP 和三维系统之间来回找信息，只需对“巡界”下达任务；系统汇集设备、环境和异常上下文，给出现场验证建议，经用户同意后，数字运维员在工程场景中跑/飞到设备，定位异常部件、指导并模拟维修，最后形成可追溯回执。

## 痛点口径

巡界解决的不是“人不会走路”，而是告警后的信息与动作断链：

1. 找设备慢：告警 ID、资产台账与空间位置不统一。
2. 判断慢：设备参数、气象、相邻设备、历史告警和 SOP 分散。
3. 到场后不标准：检查证据和步骤依赖个人经验。
4. 修完不闭环：现场动作、证据、异常状态与任务回执脱节。

## 范围锁

本轮只打透光伏 `ANOM-DEMO-01 → STR-B2-07 → INV-B-02` 一条链。

不做：水电/风电新场景、完整 3D Tiles 碰撞体、机器人/无人机真实控制、真实 SCADA、复杂多智能体框架、新的预测模型、全量测试平台。

## 五个检查点

## 当前执行状态

- [x] A：语言入口已连到既有安全任务闭环；文字主链实跑通过，浏览器语音适配已接入但未代替文字兜底。
- [x] B：高信号上下文、建议依据、重复演示复位与闭环回执已接入并完成浏览器主链复验。
- [x] C：状态一致性、首屏顺序、巡界品牌、人物飞行与维修推演已完成浏览器目检。
- [x] D：作品说明、90 秒脚本、兜底与冷启动说明已完成，并通过无上下文读者测试。
- [ ] E：08:00 前冻结与最终主链验收。

### A. 语言入口连到安全任务闭环

- 后端受控指令增加：开始检查、同意/拒绝当前审批、采集演示证据。
- 前端把这些命令接到既有 MissionRuntime，不绕过 `approvalId + contextVersion + planHash`。
- 增加浏览器按住说话/点击说话入口；不支持语音时保留文字输入并明确提示。

验收：用文字或语音依次说“检查 B2 屋顶异常”→“我同意”→“采集证据”→“我同意”，任务最终闭环。

### B. 把 AI 为什么有用展示出来

- 在批准前只显示 4 类高信号上下文：异常、设备、环境、SOP/证据要求。
- 用一句可读建议说明“为什么需要到场”，来源与 `SIMULATED/MODELED` 标签保持可见。
- 不增加虚构收益数字；只展示可追溯动作、证据和状态变化。

验收：评委不看代码也能说出“AI 在汇集上下文、提出建议和编排受控动作”。

### C. 数字人和维修成为明确 wow moment

- 保留走、跑、飞、跳、停止、第三人称跟随。
- 维修时聚焦 `STR-B2-07`，展示报警、部件展开、进度和恢复；镜头和面板不能互相遮挡关键目标。
- 场景只做必要的光伏园区质感与镜头微调，不换底层资产。

验收：一条指令后能连续看到“人物移动 → 设备聚焦 → 部件维修仿真”。

### D. 演示与提交材料

- 写一页作品说明：用户、痛点、方案、AI 作用、数据来源、落地第一步、真实性边界。
- 写 90 秒演示脚本和故障兜底脚本。
- README 保证冷启动命令、端口、演示指令清楚。

验收：陌生人按 README 可启动；讲解人按脚本不临场发明口径。

### E. 08:00 前冻结

- 只跑：前后端类型检查、受控指令单测、一次浏览器主链路。
- 不跑无关长测试；发现非致命问题记录，不在截止前重构。
- 最终检查 `git status`、演示端口、真实性标识和提交记录。

## 完成定义

- 页面冷启动可用，场景、人物和控制面板稳定出现。
- 文字主链路必过；浏览器支持时语音主链路可用。
- 用户同意仍走既有结构化审批，不用自然语言绕过安全绑定。
- 维修明确是数字现场仿真，不宣称物理维修。
- 至少有一个可恢复的提交检查点，以及作品说明和演示脚本。
