# 巡界 · 演示脚本（v2，2026-08-29 按新前端更新）

> 场景：player-demo 风电沉浸式页（AI 悬浮球面板）+ 后端 `:8787`。
> 全程文字或「按住说话」语音；未配豆包凭据时用文字，未配模型凭据时如实显示「确定性回退」（这本身是可信度卖点）。
> 口径：维修与证据统一称「数字现场仿真」；所有回答为口语化中文。

## 正常脚本（约 90 秒）

### 0–10 秒｜开场

打开风电场景页，视角先给全景：山地、10 台风机、走动中的数字运维员。

话术："一条告警出来，运维员要在告警、台账、气象、规程之间来回找信息。巡界把这件事变成一句话。"

### 10–25 秒｜上下文问答（开口就有答案）

点开右下角 AI 面板，依次说（可语音）：

- `你是谁` → 播报身份 + 当前任务状态
- `当前啥场景` → 播报场站概况（哪几台机有预警、哪台要修）

话术："它能听懂现场的问题——用人的话回答，不给编号不给术语。"

### 25–45 秒｜指挥移动（语音/文字皆可）

- `跑到 2 号风机` → 人物沿地形跑步移动（面板显示命令 chip，**不播报**）
- `飞到 7 号风机` → 直线飞行抵达问题机组

话术："移动指令直接执行，不打断你操作。说到哪就到哪，说多远飞多远。"

### 45–60 秒｜对象问答（到了设备边上问）

站在 7 号机旁：

- `它什么状态` → 播报"状态严重——齿轮箱高速端轴承磨损特征明显,已登记维修工单"
- `它的参数` → 播报机型/功率/轮毂高度/机舱长宽高

话术："到了设备边上，张嘴一问就是它的关键情况——状态、严重程度、尺寸参数，按你问的答。"

### 60–75 秒｜维修推演（Wow Moment）

- `维修 7 号风机` → 导航+聚焦+维修仿真连播：机舱定位、部件高亮、维修进度、恢复

话术："维修是标准步骤推演——数字现场仿真，先不碰真实设备。但每一步都按规程走、都留痕。"

### 75–90 秒｜收尾（可信与落地）

指向面板的「确定性回退」标识 + 回执/记录：

"大模型负责听懂和组织语言；路线、状态、数字、审批全部由确定性程序把关——模型挂了显式回退，业务不断。真实落地从接一类高频告警开始，用无效派工率、证据完整率和修复时间验证价值。"

## 光伏闭环链路（备用展示，线上站点或 API）

`检查 B2 屋顶异常` → `我同意` → 人物到位后 `采集证据` → `我同意` → 绿色闭环回执（工单 resolved、三类证据、时间与损失固化）。
该链路已由 `test:dispatch`（56 项，真实引擎）自动化保障；沉浸式光伏页在建。

## 兜底预案

- 麦克风/豆包未配置：改文字输入，链路完全相同（接口 503 并如实提示）。
- 在线模型失败：保留「确定性回退」标识继续演示，不伪称在线输出。
- 人物卡住：`回运维点` 复位位置；重说一次指令。
- 语音识别错字：面板会显示识别文本，直接用文字重发。
- 评委追问「这就是个小人跑图？」："小人只是空间执行层；核心是告警到现场作业的受控闭环——有依据、有授权、有证据、有回执。"
- 评委追问「AI 在哪」："AI 听懂并组织回答，确定性程序管路线、数字、审批；数字溯源校验让模型不能编数。"
- 评委追问「怎么落地」："先接一个场站一类高频告警（匿名导出+资产映射+SOP），用无效派工率、研判耗时、MTTR 验证。"

## 演示前检查

1. `engine` 与 `player-demo` 两个 dev 服务已起；面板显示「场景就绪：老鸦岭风电场…10 台风机已登记」。
2. 试说一句 `你是谁`，确认回答与播报正常；TTS 开关按需。
3. 豆包/模型凭据按需配置；未配置时提前想好「确定性回退」话术。
